spring:
  datasource:
    url: jdbc:postgresql://db.zecthmgsgthywfgxvnpj.supabase.co:5432/postgres
    username: postgres
    password: Damnfarny12!!
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate.format_sql: true
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

  # H2 Configuration (commented out for Supabase)
  # h2:
  #   console:
  #     enabled: true
  #     path: /h2-console
  #     settings:
  #       web-allow-others: true

management:
  endpoints:
    web:
      exposure:
        include: health,info

security:
  jwt:
    # at least 32 chars
    secret: dev-please-change-this-32-bytes-min!!!!
    expiry-seconds: 86400

web:
  cors:
    allowed-origins: "http://localhost:3000,http://localhost:5173"
aqms:
  queue:
    expressFraction: 0.333
    emergencyFraction: 0.125
  rules:
    minAdvanceHoursForChange: 24
