spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/clinicdb?sslmode=disable
    username: postgres         # <-- put your DB user (e.g., clinicuser)
    password: bestcoder123     # <-- put your DB password
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate.format_sql: true

  h2:
    console:
      enabled: true

management:
  endpoints:
    web:
      exposure:
        include: health,info

security:
  jwt:
    # at least 32 chars
    secret: dev-please-change-this-32-bytes-min!!!!
    expiry-seconds: 86400

web:
  cors:
    allowed-origins: "http://localhost:3000,http://localhost:5173"
aqms:
  queue:
    expressFraction: 0.333
    emergencyFraction: 0.125
  rules:
    minAdvanceHoursForChange: 24
