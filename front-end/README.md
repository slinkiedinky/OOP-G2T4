AQMS front-end (Next.js)

Dev:
1. cd front-end
2. npm install
3. npm run dev

This project uses Next.js App Router. It proxies `/api/*` to the backend at http://localhost:8080 via `next.config.js` rewrites.
